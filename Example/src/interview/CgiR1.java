package interview;

import java.util.Comparator;
import java.util.List;
import java.util.stream.Collectors;

public class CgiR1 {
    public static void main(String[] args) {
       /* System.out.println("CGI");
        List<Emp> emp
        List<Emp> resultList = emp.stream().filter(e -> e.getSalary() > 40000).reduce(Emp::getId,(a,b) -> a+b));
        emp.stream().max(Comparator.comparingDouble(Emp::getId)).get();*/


    }
}

